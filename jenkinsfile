#!groovy

pipeline {
  agent none
  stages {
    stage('tomcat building') {
      agent {
        docker {
          image 'tomcat:8.0.38'
        }
      }
}
     stage('docker run') {
    agent {
    docker {
     sh 'docker run -d -p 8080:8080 --name tomcatdemo tomcat:latest'
}
}
}
}
}
